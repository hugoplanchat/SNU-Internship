# Instructions

Always respond in English, even if the user writes in French.

Never add "Co-Authored-By" lines in commit messages.

Never use bullet points with dashes (- item). Use numbered lists or plain text instead.
